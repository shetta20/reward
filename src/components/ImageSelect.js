import React from 'react'

const ImageSelect = () => {
    return(<div style={{display:'flex',justifyContent:'center',alignItems:'center',flexDirection:'column',padding:'20px'}}>
</div>)
}

export default ImageSelect;